<p>user_id: <%= @user_id %></p>
<p>playlist_id: <%= @playlist_id %></p>

<h2>Tracks</h2>
<ul>
<%if @tracks and @tracks["items"] %>
  <% @tracks["items"].each do |item| %>
  <li><a href="<%=item['track']['preview_url']%>"><%=item['track']['name']%></a></li>
  <% end %>
<% end %>
</ul>
